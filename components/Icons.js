export const IconXS = ({ Icon, color }) => {
  return (
    <Icon className={`h-8 w-8 text-[${color}]`} />
  )
}
export const IconSM = ({ Icon, color }) => {
  return (
    <Icon className={`h-16 w-16 text-[${color}]`} />
  )
}
